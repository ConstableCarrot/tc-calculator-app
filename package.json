{
  "name": "tc-calculator-app",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "sst start",
    "build": "sst build",
    "deploy": "sst deploy",
    "remove": "sst remove",
    "console": "sst console",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@serverless-stack/cli": "^1.2.11",
    "@serverless-stack/resources": "^1.2.11",
    "@tsconfig/node16": "^1.0.2",
    "typescript": "^4.7.2",
    "vitest": "^0.12.9"
  },
  "workspaces": [
    "backend"
  ],
  "dependencies": {
    "@sls-next/lambda-at-edge": "^3.7.0"
  }
}
